<nav class='navbar navbar-default'>
  <div class='container'>
    <div class='navbar-brand'>CRM-System</div>
    <ul class="nav navbar-nav">
      {{#link-to 'welcome' class="nav-link" tagName="li"}}<a href><i class="fa fa-home fa-fw" aria-hidden="true"></i>&nbsp; Home</a>{{/link-to}}
    </ul>
    <ul class="nav navbar-nav">
      {{#if session.isAuthenticated}} {{#link-to 'admin' class="nav-link" tagName="li"}}<a href><i class="fa fa-cog fa-fw" aria-hidden="true"></i>&nbsp; Admin</a>{{/link-to}} {{/if}}
    </ul>
    <div class='navbar-right'>
      {{#if session.isAuthenticated}}
      <button class='btn btn-default navbar-btn' {{action 'signOut'}}>
                    {{fa-icon 'sign-out'}} Sign Out
                </button> {{/if}}
    </div>
  </div>
</nav>
<div class='container'>
  {{outlet}}
</div>